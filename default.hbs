<!DOCTYPE html>
<html lang="{{@site.locale}}">
<head>

    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{asset "built/index.css"}}" />
    <script src="{{asset "built/index.js"}}" defer></script>

    <title>{{meta_title}}</title>

    {{ghost_head}}
    {{!-- Outputs important meta data and settings, should always be in <head> --}}

    {{!-- Load Roboto font from Google Fonts --}}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap&subset=latin-ext" rel="stylesheet">

    {{!-- Inject custom theme settings as CSS variables --}}
    <style>
        :root {
            {{#if @custom.secondary_accent_color}}--secondary-accent-color: {{@custom.secondary_accent_color}};{{/if}}
            {{#if @custom.featured_overlay_color}}--featured-overlay-color: {{@custom.featured_overlay_color}};{{/if}}
            {{#if @custom.navigation_text_color}}--navigation-text-color: {{@custom.navigation_text_color}};{{/if}}
        }
    </style>

    {{!-- Removed PhotoSwipe CSS Link --}}

</head>
<body class="{{body_class}}">
<div class="gh-viewport">

<header id="gh-head" class="gh-head">
    <nav class="gh-head-inner gh-container">

        <div class="gh-head-brand">
            <a class="gh-head-logo" href="{{@site.url}}">
                {{#if @site.logo}}
                    <img src="{{@site.logo}}" alt="{{@site.title}}" />
                {{else}}
                    {{@site.title}}
                {{/if}}
            </a>
            <a class="gh-burger" role="button" aria-label="menu">
                <div class="gh-burger-box">
                    <div class="gh-burger-inner"></div>
                </div>
            </a>
        </div>
        <div class="gh-head-menu">
            {{navigation}}
        </div>
        <div class="gh-head-actions">
            <div class="gh-head-actions-list">
                {{search}}
                {{#if @site.members_enabled}}
                    {{^if @member}}
                    <a class="gh-button" href="{{@site.signup_url}}">Subscribe</a>
                    {{else}}
                    <a class="gh-button" href="#/portal/account">Account</a>
                    {{/if}}
                {{/if}}
            </div>
        </div>
    </nav>
</header>

<main class="gh-main">

    {{{body}}}
    {{!-- All content gets inserted here, index.hbs, post.hbs, etc --}}

</main>

<footer class="gh-foot">
    <div class="gh-container">
        <div class="gh-foot-menu">
            {{navigation type="secondary"}}
        </div>
        <div class="gh-foot-social"> {{!-- Added social links section --}}
            {{#if @site.facebook}}
                <a href="{{@site.facebook_url}}" target="_blank" rel="noopener" aria-label="Facebook">{{> "icons/facebook"}}</a>
            {{/if}}
            {{#if @site.twitter}}
                <a href="{{@site.twitter_url}}" target="_blank" rel="noopener" aria-label="Twitter">{{> "icons/twitter"}}</a>
            {{/if}}
            {{!-- Add other social links like Instagram, LinkedIn if needed, checking for @site variables --}}
            {{!-- Example: <a href="INSTAGRAM_URL" target="_blank" rel="noopener">Instagram</a> --}}
            {{!-- Example: <a href="LINKEDIN_URL" target="_blank" rel="noopener">LinkedIn</a> --}}
            <a href="{{@site.url}}/rss/" target="_blank" rel="noopener" aria-label="RSS Feed">{{> "icons/rss"}}</a> {{!-- Keep RSS --}}
        </div>
        <div class="gh-foot-meta">
            Published with <a href="https://ghost.org" target="_blank" rel="noopener">Ghost</a>
        </div>
    </div>
</footer>

</div>

{{!-- <foot> --}}
{{ghost_foot}}
{{!-- Outputs important scripts - should always be included before closing body tag --}}

{{!-- Removed PhotoSwipe JS module script tag, it's imported in index.js now --}}

</body>
</html>
